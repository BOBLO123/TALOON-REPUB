// x is the id of the form, y is the requirements for a name.
function check(x, y){
  // id should have quotes around it
  value = document.getElementById(x).value
  if (value = (y)){
    // put what you want it to do here
  }
}
function signup(x, y){
  //haven't done thi yet
